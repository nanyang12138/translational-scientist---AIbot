{
  "name": "god-is-offline-agent-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "test": "node --test && npm run validate:godot",
    "validate:godot": "node tools/validate_godot_project.mjs",
    "export:windows": "./tools/export_windows_release.sh"
  }
}
